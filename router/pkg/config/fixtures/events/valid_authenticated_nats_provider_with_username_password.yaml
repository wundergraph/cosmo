# yaml-language-server: $schema=../../config.schema.json

version: "1"

graph:
  token: "token"

events:
  providers:
    nats:
      - id: default
        url: "nats://localhost:4222"
        authentication:
          username: "username"
          password: "password"
