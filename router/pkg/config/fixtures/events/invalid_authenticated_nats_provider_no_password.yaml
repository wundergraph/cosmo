# yaml-language-server: $schema=../../config.schema.json

version: "1"

graph:
  token: "token"

events:
  sources:
    default:
      provider: NATS
      url: "nats://localhost:4222"
      authentication:
        username: "username"