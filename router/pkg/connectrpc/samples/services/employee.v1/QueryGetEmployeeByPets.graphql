query FindEmployeesByPets($hasPets: Boolean!) {
    findEmployees(criteria: {hasPets: $hasPets}) {
        id
        details {
            forename
            surname
            pets {
                name
            }
        }
    }
}
