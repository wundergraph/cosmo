query FindEmployees($criteria: SearchInput) {
  findEmployees(criteria: $criteria) {
    currentMood
    details {
      forename
      hasChildren
      maritalStatus
      nationality
      pets {
        name
        gender
      }
    }
    products
  }
}