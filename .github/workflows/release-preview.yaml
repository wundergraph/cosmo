name: Release Preview

on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/release-preview.yaml'
      - '.github/workflows/release.yaml'
  push:
    tags-ignore:
      - '**'
    branches:
      - 'main'
    paths-ignore:
      - 'docs*/**'
      - '*.md'
      - '.github/CODEOWNERS'

concurrency:
  group: ${{github.workflow}}-${{github.head_ref}}
  cancel-in-progress: false

env:
  CI: true
  DO_NOT_TRACK: '1'

jobs:
  pull-request-preview:
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          persist-credentials: false

      - uses: pnpm/setup@84cb39b217b10273981911c288cd62326dc7c6d2 # v2
        with:
          cache: true
          install: false

      - name: Install
        run: pnpm install --frozen-lockfile --ignore-scripts --filter='wundergraph-cosmo'

      - name: Generate next release (dry-run)
        run: |
          git checkout -b release-preview
          pnpm lerna version --ignore-scripts --dry-run --yes --no-push --no-git-tag-version
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Show release preview
        run: |
          {
            echo '## Release preview'
            echo 'Versions and changelogs generated from this PR to test the release workflow.'
            echo '```diff'
            git --no-pager diff -- './*/package.json' './*/CHANGELOG.md'
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

  version:
    if: github.event_name == 'push'
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - name: Authenticate
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ secrets.APP_CLIENT_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          repositories: |
            cosmo
          permission-actions: write
          permission-contents: write
          permission-workflows: write

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ steps.app-token.outputs.token }}

      - uses: pnpm/setup@84cb39b217b10273981911c288cd62326dc7c6d2 # v2
        with:
          cache: true
          install: false

      - run: npm config set //registry.npmjs.org/:_authToken $NPM_TOKEN
        shell: bash
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Install
        run: pnpm install --frozen-lockfile --filter='wundergraph-cosmo'

      - name: Generate next release (dry-run)
        run: pnpm run release-preview --yes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Show CHANGELOG.md
        run: echo -e "\`\`\`diff\n$(git --no-pager diff './*/CHANGELOG.md')\n\`\`\`" >> $GITHUB_STEP_SUMMARY
