query {
  employee(id: 1) {
    id
    details { forename }
    ... @defer {
      details { location { language } }
      hobbies { __typename }
    }
  }
  teammates(team: OPERATIONS) {
    id
    details { forename }
    products
  }
}
