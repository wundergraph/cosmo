query {
  products {
    upc
    ... on Cosmo { name repositoryURL }
  }
  ... @defer {
    productTypes {
      __typename
      ... on Documentation { url(product: COSMO) }
      ... on Consultancy { name lead { details { forename } } }
    }
  }
}
