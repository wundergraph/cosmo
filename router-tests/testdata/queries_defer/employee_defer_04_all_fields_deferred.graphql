query {
  ... @defer {
    employee(id: 1) {
      id
      details { forename location { language } }
      hobbies { __typename }
    }
    teammates(team: OPERATIONS) {
      id
      details { forename }
      products
    }
  }
}
