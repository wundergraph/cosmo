# CLAUDE.md

## Documentation Requirements

When making changes that involve **new features**, **configuration updates**, or **architectural changes**, check if the documentation in `docs-website/` needs to be updated. The docs directory structure mirrors the codebase — find the relevant section and update accordingly.

New pages must be added to the navigation in `docs-website/docs.json`. Preview locally with `cd docs-website && mintlify dev`.
