{
  "name": "defer-demo-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TypeScript HTTP test suite exercising the cosmo router @defer implementation across federation v2 features.",
  "scripts": {
    "test": "vitest run --reporter=verbose"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
