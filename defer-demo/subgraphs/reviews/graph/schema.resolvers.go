package graph

// This file will be automatically regenerated based on the schema, any resolver
// implementations
// will be copied through when generating and any unknown code will be moved to the end.
// Code generated by github.com/99designs/gqlgen version v0.17.91

import (
	"context"
	"deferdemo/reviews/graph/model"
	"encoding/json"
	"math"
)

// ReadingTimeAdjusted is the resolver for the readingTimeAdjusted field.
//
// @requires(fields: "article { wordCount }") — gqlgen passes the required
// external fields in federationRequires (JSON-decoded, untyped). Formula
// (FIXTURES §5): readingTimeAdjusted = ceil(wordCount / 200) + rating.
func (r *reviewResolver) ReadingTimeAdjusted(ctx context.Context, obj *model.Review, federationRequires map[string]any) (int, error) {
	wordCount := wordCountFromRequires(federationRequires)
	adjusted := int(math.Ceil(float64(wordCount)/200.0)) + obj.Rating
	return adjusted, nil
}

// wordCountFromRequires extracts article.wordCount from the federationRequires
// map. JSON numbers decode to float64. Returns 0 when absent.
func wordCountFromRequires(federationRequires map[string]any) int {
	article, ok := federationRequires["article"].(map[string]any)
	if !ok {
		return 0
	}
	switch wc := article["wordCount"].(type) {
	case float64:
		return int(wc)
	case int:
		return wc
	case int64:
		return int(wc)
	case json.Number:
		n, _ := wc.Int64()
		return int(n)
	}
	return 0
}

// Review returns ReviewResolver implementation.
func (r *Resolver) Review() ReviewResolver { return &reviewResolver{r} }

type reviewResolver struct{ *Resolver }
