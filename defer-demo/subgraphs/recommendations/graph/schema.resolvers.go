package graph

// This file will be automatically regenerated based on the schema, any resolver
// implementations
// will be copied through when generating and any unknown code will be moved to the end.
// Code generated by github.com/99designs/gqlgen version v0.17.91

import (
	"context"
	"time"

	"deferdemo/recommendations/graph/model"
)

// RelatedContent is the resolver for the relatedContent field.
func (r *articleResolver) RelatedContent(ctx context.Context, obj *model.Article) ([]model.Publishable, error) {
	time.Sleep(deferLatency) // mock expensive ML recommendation (DESIGN §6)
	refs := relatedContentMembers[obj.ID]
	out := make([]model.Publishable, 0, len(refs))
	for _, ref := range refs {
		out = append(out, newPublishable(ref))
	}
	return out, nil
}

// RecommendedArticles is the resolver for the recommendedArticles field.
func (r *userResolver) RecommendedArticles(ctx context.Context, obj *model.User) ([]*model.Article, error) {
	time.Sleep(deferLatency) // mock expensive ML recommendation (DESIGN §6)
	ids := recommendedArticleIDs[obj.ID]
	out := make([]*model.Article, 0, len(ids))
	for _, id := range ids {
		out = append(out, &model.Article{ID: id})
	}
	return out, nil
}

// Article returns ArticleResolver implementation.
func (r *Resolver) Article() ArticleResolver { return &articleResolver{r} }

// User returns UserResolver implementation.
func (r *Resolver) User() UserResolver { return &userResolver{r} }

type articleResolver struct{ *Resolver }
type userResolver struct{ *Resolver }
