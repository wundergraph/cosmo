# yaml-language-server: $schema=./pkg/config/config.schema.json

# See pkg/config/config.go for the full list of configuration options.
# This file is used for the demo environment

version: "1"

# profiling:
#     enabled: true
#     server_address: "http://pyroscope:4040"
#     profile_types:
#       - "cpu"
#       - "alloc_objects"
#       - "alloc_space"
#       - "inuse_objects"
#       - "inuse_space"
#     tags:
#       environment: local
#       run_id: 1

engine:
  # max_concurrent_resolvers: 5048
  debug:
    report_websocket_connections: true
    report_memory_usage: true

    