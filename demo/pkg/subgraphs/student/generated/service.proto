syntax = "proto3";
package service;

option go_package = "github.com/wundergraph/cosmo/plugin";

// Service definition for StudentService
service StudentService {
  // The hello query
  rpc QueryHello2(QueryHello2Request) returns (QueryHello2Response) {}
}

// Request message for hello2 operation: The hello query.
message QueryHello2Request {
  string name = 1;
}
// Response message for hello2 operation: The hello query.
message QueryHello2Response {
  // The hello query
  World hello_2 = 1;
}

message World {
  // The ID of the world
  string id = 1;
  // The name of the world
  string name = 2;
}